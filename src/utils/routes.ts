enum Routes {
    HOME = '/',
    LOGIN = '/login',
    REGISTER = '/register',
    DASHBOARD = '/dashboard',
    PROFILE = '/profile',
    SETTINGS = '/settings',
    NOT_FOUND = '/404',
}

export default Routes;